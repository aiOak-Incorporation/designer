[start]
  cmd = "python -m pip install --no-cache-dir psycopg2-binary==2.9.9 && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn designer.wsgi:application --bind 0.0.0.0:8016 --workers ${WORKERS:-3}"